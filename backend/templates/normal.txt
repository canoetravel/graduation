#include <stdio.h>

int main() {
    printf("Hello, OS Auto Judge!\n");
    
    int sum = 0;
    for (int i = 1; i <= 100; i++) {
        sum += i;
    }
    printf("Sum from 1 to 100 is: %d\n", sum);
    
    return 0;
}